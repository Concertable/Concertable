# Big review — Feature/BookingAgreement

**Plan anchored to commit:** `3cc9f8749d3ad23b7f458bc74d8a9a09978cef62`  _(2026-07-08)_
**Reviewed up to commit:** `3cc9f8749d3ad23b7f458bc74d8a9a09978cef62`  _(2026-07-08)_
Net diff reviewed: `97b3c758..3cc9f874` (131 files, +2148/-141). Move-only files skipped.
Status legend: `[ ]` not yet reviewed · `[x]` reviewed (date) · `[~]` in progress (incomplete — re-review).

## Summary — all 7 areas reviewed (2026-07-08)

**Big review complete.** 7 findings, **all LOW**, no CRITICAL/HIGH/MEDIUM. Nothing blocks merge; every finding is a judgement-call consistency/hygiene nit. All 6 cross-area notes checked and resolved (consent non-null, fingerprint recompute-and-compare guard, `INotifier→IMessenger` rename, FE click-wrap flag on all surfaces, tests updated, `Legal:PlatformTermsVersion` config present).

By lens: **code-patterns / C# conventions** 6 (CV1–CV5 + none), **correctness (weak test)** 1 (TEST1). No microservice-isolation, module-boundary, or seeding violations found anywhere in the diff. The keyed-strategy-resolver pattern, two-party tenant filter (fail-closed 404), snapshot immutability, and atomic Accept transaction are all implemented correctly.

Findings worth the author's glance (all optional):
- **CV1 / CV2** — money-scalar / fingerprint extraction branch on concrete contract subtypes (cast-cluster / `switch`) in otherwise contract-agnostic code, where the sibling `AgreementTermsRenderer` uses the committed keyed-strategy shape. In-feature inconsistency, not a bug.
- **CV3 / CV5** — two doc comments use `/* */` / bare `///` instead of `/// <summary>`; content is legit WHY, just wrap it.
- **CV4** — `GetByBookingIdIgnoringTenantAsync` uses bare `.IgnoreQueryFilters()`; follows existing `BookingRepository` precedent but prefer the named-key `IgnoreQueryFilters([TenantFilters.Key])` form.
- **TEST1** — one metadata-endpoint assertion substring-matches `"2026-07"`, which also matches `CreatedAtUtc` — the exact-value check exists elsewhere (`AssertCommonSnapshot`), so it's a latent test blind spot, not a live gap.

## Coverage
- [x] Concert Domain + Infrastructure Data/schema (2026-07-08) — foundation — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Data` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Schema.cs` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/LegalSettings.cs` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Concertable.B2B.Concert.Infrastructure.csproj` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Extensions`
- [x] Concert Application — interfaces/DTOs/mappers/renderers (2026-07-08) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Application`
- [x] Concert Infrastructure Services + PDF + Repositories (2026-07-08) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Services` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Pdf` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Repositories`
- [x] Concert API layer — controllers/requests/responses/validators/mappers (2026-07-08) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Api`
- [x] Frontend (app/) — reviewed 2026-07-08 — `app/shared` `app/web/b2b`
- [x] Tests — Concert integration/unit + B2B E2E (2026-07-08) — `api/Concertable.B2B/src/Modules/Concert/Tests` `api/Concertable.B2B/tests`
- [x] Migrations, packages & infra plumbing (2026-07-08) — `**/Migrations/**` `**/Directory.Packages.props` `api/initial-migrations.ps1` `api/Concertable.Messaging/Concertable.Messaging.Infrastructure/Concertable.Messaging.Infrastructure.csproj` `api/Concertable.B2B/src/Concertable.B2B.Web/appsettings.json` `api/Concertable.B2B/TECH_DEBT.md` `plans/b2b/LAUNCH_PLAN.md` `reviews/Feature-BookingAgreement.md`

## Cross-area notes
<!-- one-liners added during a stage for things a LATER stage must verify -->
- ~~**[for: Migrations/plumbing stage]** Verify `api/Concertable.B2B/src/Concertable.B2B.Web/appsettings.json` defines `Legal:PlatformTermsVersion`. It binds to `LegalSettings` (default `null!`) and is persisted verbatim into the **immutable** `BookingAgreementEntity.PlatformTermsVersion`; a missing config key would write `null` into a permanent legal record.~~ **RESOLVED** — `appsettings.json` defines `"Legal": { "PlatformTermsVersion": "2026-07" }`, matching the value the integration tests assert (`AssertCommonSnapshot` → `Assert.Equal("2026-07", agreement.PlatformTermsVersion)`). The key is present; no null is written.
- ~~**[for: Concert Infrastructure Services + PDF stage]** `BookingAgreementEntityConfiguration` makes `VenueConsent` a **required** owned type. Verify the Accept path always supplies a non-null `VenueConsent`.~~ **RESOLVED** — `BookingAgreementBuilder.BuildAsync` always constructs `new Consent(currentUser.Id, …)` for the venue side (never null); a null would only arise if `currentUser.Id` were null, which throws `ForbiddenException` first.
- ~~**[for: Concert Infrastructure Services + PDF stage]** `BookingAgreementEntity.Create` extracts per-contract money via casts (see CV1). Check whether `BookingAgreementBuilder` resolves these via a keyed strategy and could pass scalars in.~~ **RESOLVED** — `BookingAgreementBuilder` forwards the whole `IContract` to `Create` and only uses the keyed strategy for the terms *text* (`termsRenderer.Render`); it does **not** pre-resolve the money scalars, so CV1 stands as originally written (the cast-cluster remains in the domain factory).
- ~~**[for: Concert Infrastructure Services + PDF stage]** Verify the Accept path **recomputes and compares** the fingerprint (rejects on mismatch).~~ **RESOLVED** — `AcceptExecutor.VerifyTermsUnchanged` recomputes `termsFingerprint.Calculate(contract, app.Opportunity.Period)` and compares against the stored `app.TermsFingerprint`, throwing `BadRequestException` on mismatch. Runs *before* the money-moving accept step (guarded correctly). Null stored fingerprint (pre-click-wrap) skips the guard, as intended.
- ~~**[for: Concert Infrastructure Services + PDF stage]** `INotifier` → `IMessenger` — confirm impl + DI renamed, no stale reference.~~ **RESOLVED** — `Notifier`→`Messenger` class renamed, `services.AddScoped<IMessenger, Messenger>()` updated (line 80); no stale `INotifier` reference remains (the unrelated `IApplicationNotifier`/`IConcertNotifier` are separate types, untouched by the rename).
- ~~**[for: Frontend stage]** `ApplyRequest`/`AcceptRequest` are now **required bodies** with a mandatory `AgreedToTerms == true` (FluentValidation auto-validation is globally on via `AddFluentValidationAutoValidation()`). The FE apply/accept calls must post `{ agreedToTerms: true, … }` — a missing/false flag now returns **400** ("You must agree to the contract terms"), where the old optional-body call succeeded. Verify the web client sends it.~~ **RESOLVED** — all three `applicationApi` calls now post the flag: `applyToOpportunity` → `{ agreedToTerms: true }`, `applyToOpportunityWithPayment` → `{ agreedToTerms: true, paymentMethodId }`, `acceptApplication` → `{ agreedToTerms: true, ...body }`. Sent unconditionally at the API layer; the click-wrap is enforced in the UI by disabling the submit/apply button until `AgreeToTermsCheckbox` is ticked, on all four surfaces.
- ~~**[for: Tests stage]** Same signature change: any existing test that POSTed `apply`/`accept` with no body (previously allowed via `ApplyRequest? = null`) now gets 400. Verify tests were updated to send `AgreedToTerms = true`.~~ **RESOLVED** — every `apply`/`accept` POST across the whole B2B test tree now sends `{ agreedToTerms = true }` (integration: Application/Cancel/DoorSplit/FlatFee/VenueHire/Versus + Concert/Cancel + Outbox + TenantScoping; API E2E: ConcertCancelled/ConcertDraft). Swept for stragglers — the only bodyless `PostAsync` calls left target `cancel`/`withdraw`/`reject`/`checkout`, none of which gained a required body. Zero unconverted call sites.

## Findings
<!-- appended per area; finding IDs continue across areas: MS#, MB#, BUG#, SEED#, CV# -->

## Concert Domain + Infrastructure Data/schema — reviewed 2026-07-08

Foundation stage. Reviewed the new `BookingAgreementEntity` / `Consent` domain types, `ApplicationEntity` consent fields, EF configs, `ConcertDbContext`/`Schema`/`ConcertConfigurationProvider` wiring, `LegalSettings`, DI registrations, and the `.csproj` package adds (QuestPDF, `Shared.Blob.Application`, `Shared.Pdf.Application`).

**Microservice isolation:** clean — no cross-data-service runtime references; new package refs are agnostic shared libs (Blob/Pdf/QuestPDF). **Seeding:** `BookingAgreementEntity` is a write model created inside the Accept transaction (not a projection/handler-owned row), so no seeding-convention concern here. **FK:** `BookingAgreement → Booking` FK targets a write-model entity in the same context — not the read-model-FK anti-pattern.

- [ ] **CV1 — LOW — C# conventions / code-patterns** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain/Entities/BookingAgreementEntity.cs:66-73`
  `Create` branches on concrete contract subtypes to flatten money fields (`(contract as FlatFeeContract)?.Fee`, `(contract as VenueHireContract)?.HireFee`, `(contract as VersusContract)?.Guarantee`, and a `switch { DoorSplitContract … VersusContract … }` for `ArtistDoorPercent`). `CODE_PATTERNS.md` ("Keyed strategy resolver" → anti-patterns) says rules that **vary by `ContractType` never branch/cast in agnostic code** — they go through one keyed resolver. This feature already uses that shape for the `*TermsRenderer` strategies, so the cast-cluster here is an inconsistency. _Counter-argument (why it's LOW, not a hard bug):_ this is a single domain-factory site producing a persisted snapshot, not a rule "planted where nobody looks," and the alternative (four resolver methods each returning null for non-matching types) is arguably noisier. Judgement call for the author — flagging for consistency with the committed pattern. See the Stage-3 cross-area note to confirm the builder can't pass these in as scalars.

No other issues found in this area (correctness, microservice isolation, module boundaries, seeding all clean).

## Concert Application — interfaces/DTOs/mappers/renderers — reviewed 2026-07-08

Reviewed the new `IAgreementTermsRenderer`/`IBookingAgreementBuilder`/`IBookingAgreementPdfService`/`IBookingAgreementRepository`/`IBookingAgreementService`/`IClientContext`/`ITermsFingerprintCalculator` interfaces, the `INotifier`→`IMessenger` rename, the `BookingAgreementDto`/`ConsentDto`/`AgreementPdf` DTOs + `ApplicationDto.AgreementId` add, the `ApplicationMapper` refactor (agreement-id lookup, extracted `ApplicationMappers` extensions), and the five `*TermsRenderer` + `AgreementTermsRenderer` facade + `TermsFingerprintCalculator` + `AgreementTermsFormat`.

**Keyed strategy resolver:** `AgreementTermsRenderer` is a textbook implementation of the `CODE_PATTERNS.md` shape — concrete strategy ctor params, `FrozenDictionary<ContractType, …>`, `renderers[contract.ContractType].Render(...)` (unmapped key throws). Clean. **Microservice isolation / module boundaries:** clean — Application depends only on its own Domain + `B2B.Contract.Contracts` + `B2B.DataAccess.Application`; no cross-data-service refs. **Correctness:** `ApplicationMappers.ToStatus` now enumerates all 10 `LifecycleState` members explicitly and throws on an unlisted one; verified against the enum — exhaustive, every existing state maps exactly as the old `_ => Accepted` default did, so the `throw` is fail-loud-on-future-member only, not a regression. `ToDtosAsync` uses a single batched `GetIdsByApplicationIdsAsync` (no N+1). **Seeding:** n/a (no seeders in this area).

- [ ] **CV2 — LOW — code-patterns (keyed strategy resolver)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Application/Renderers/TermsFingerprintCalculator.cs:12-19`
  `Calculate` branches on the concrete contract subtype with a `contract switch { FlatFeeContract c … VenueHireContract c … DoorSplitContract c … VersusContract c … }` inside an otherwise contract-agnostic component. `CODE_PATTERNS.md` ("Keyed strategy resolver" → anti-patterns → "Branching on the key in agnostic components") says per-`ContractType` logic goes through one keyed resolver, never a switch in agnostic code. The *sibling* `AgreementTermsRenderer` in the same folder does exactly this via keyed strategies (`FlatFeeTermsRenderer`, etc.), so the switch here is an in-feature inconsistency — the per-type "numbers" extraction could be a `Fingerprint(IContract)` method on the same strategy set. _Counter-argument (why LOW, same as CV1):_ it's a single discoverable calculator, not a rule scattered across handlers, and the switch is exhaustive with a loud `ArgumentOutOfRangeException` default. Judgement call for the author; flagging for consistency with the committed pattern.

- [ ] **CV3 — LOW — C# conventions (doc comments)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Application/Interfaces/IBookingAgreementRepository.cs:8-19`
  The two finder methods are documented with `/* … */` block comments. `CODE_CONVENTIONS.md` ("Doc comments — XML `<summary>`, not `//`") says member documentation is written as an XML `/// <summary>` doc comment, reserving non-XML comments for short inline notes inside method bodies. Every sibling interface added in this same diff (`IClientContext`, `IMessenger`, `IBookingAgreementPdfService`, `IBookingAgreementBuilder`, `IAgreementTermsRenderer`, `ITermsFingerprintCalculator`) uses `/// <summary>`, so this is the lone form-inconsistency; the content itself (tenant-filter rationale per finder) is legitimate WHY worth keeping — just convert to `<summary>`.

## Concert Infrastructure Services + PDF + Repositories — reviewed 2026-07-08

Reviewed `BookingAgreementBuilder`, `BookingAgreementService`, `BookingAgreementPdfService`, `ClientContextAccessor`, the `Notifier`→`Messenger` rename, `BookingAgreementDocument` (QuestPDF), `BookingAgreementRepository`, the `OpportunityRepository.GetPeriodByIdAsync` add, and the `AcceptExecutor`/`ApplyExecutor` changes (consent capture at Apply, fingerprint guard + agreement build + background PDF render at Accept).

**Correctness:** solid throughout.
- **Atomicity ✓** — `BookingAgreementBuilder.AddAsync` only tracks (base `Repository.AddAsync` does not `SaveChanges`); the agreement, the `Accept()` transition, and consent all commit together via the transitioner's single `applicationRepository.SaveChangesAsync()` on the shared scoped `ConcertDbContext`.
- **No NRE ✓** — both `AcceptExecutor.VerifyTermsUnchanged` and `BookingAgreementBuilder` dereference `app.Opportunity.Period`; `ApplicationRepository.GetByIdAsync` (the transitioner's loader) eager-`Include`s `Opportunity`.
- **Background PDF race is non-fatal by design ✓** — the `GenerateForBookingAsync` background task is enqueued in the effect (before the transition commits) but early-returns if the agreement row isn't visible yet or the blob already exists; the download path (`GetOrCreateAsync`) lazily regenerates, so a lost race or blob outage never orphans the download.
- **Download security ✓** — `BookingAgreementEntity` is tenant-filtered (`ApplyVenueArtist<BookingAgreementEntity>` in `ConcertDbContext:36`), so `GetByApplicationIdAsync` returns null to a non-party → 404, exactly as the service comment claims.

**Microservice isolation / module boundaries:** clean — all deps are same-service (Concert Application/Domain/Data + `Shared.Blob`/`Shared.Pdf`/`Kernel`); no cross-data-service refs; `IPdfService`/`IBlobStorageService` are agnostic shared adapters. **Keyed strategy:** `BookingAgreementBuilder` correctly uses `IAgreementTermsRenderer` (the keyed facade) for terms text. **DI:** all six new services + `LegalSettings` config are registered. **Seeding:** n/a.

- [ ] **CV4 — LOW — code-patterns (tenancy is composed, never subtracted)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Repositories/BookingAgreementRepository.cs:18`
  `GetByBookingIdIgnoringTenantAsync` uses a bare `.IgnoreQueryFilters()`. `CODE_PATTERNS.md` ("Tenancy is composed, never subtracted") states *"Per-query `IgnoreQueryFilters` calls are banned … The codebase has zero `IgnoreQueryFilters` calls"* — visibility should come from the context a repo is built on (a `Public*`/cross-tenant stance), not from stripping filters per query. The need here is legitimate (the background PDF render runs on a tenant-less scope and must find the row regardless of tenant). _Counter-argument (why LOW, not a hard finding):_ master **already** carries an identical bare `IgnoreQueryFilters()` in `BookingRepository.cs:75` (the "zero calls" claim in the doc is already stale), so this follows existing precedent rather than introducing a novel anti-pattern. If kept, prefer the surgical named-key form the doc itself references — `IgnoreQueryFilters([TenantFilters.Key])` (`TenantFilters.cs:8`) — which lifts only the tenant filter instead of every global filter. Judgement call for the author.

No other issues found in this area (correctness, microservice isolation, module boundaries, seeding all clean).

## Concert API layer — controllers/requests/responses/validators/mappers — reviewed 2026-07-08

_(Note: HEAD is now `4e958dd4`, one docs-only commit past the plan anchor `3cc9f874`; it touches only `plans/`, so this stage was reviewed against the anchored net diff unchanged.)_

Reviewed the `ApplicationController` changes (new `IBookingAgreementService` dep, two agreement read endpoints, `Apply`/`Accept` signature changes), the new `ApplicationValidators` (FluentValidation click-wrap), `ServiceCollectionExtensions` validator registration, `ApplicationRequests`/`ApplicationResponses` record changes, and the `ApplicationResponseMapper` HATEOAS `Agreement` link.

**Correctness:** solid.
- **Click-wrap actually enforced ✓** — `AddFluentValidationAutoValidation()` is registered globally at the B2B.Web host (`Concertable.B2B.Web/Extensions/ServiceCollectionExtensions.cs:60`), and the module registers the validators with `AddValidatorsFromAssemblyContaining<ApplyRequestValidator>(includeInternalTypes: true)` — the `includeInternalTypes` flag is required and correct because the requests/validators are `internal`. So `RuleFor(x => x.AgreedToTerms).Equal(true)` fires on model binding; a false/missing flag → 400 before the action body runs.
- **Required-body change is safe ✓** — `Apply`/`Accept` moved from optional `ApplyRequest? = null` to a required `[FromBody] ApplyRequest`. Non-nullable reference-type body params are implicitly required in MVC, so an empty body → 400 (not an NRE on `request.PaymentMethodId`). Behaviour change is intentional (consent capture); flagged forward to the FE + Tests stages (cross-area notes).
- **Agreement reads fail closed ✓** — `GetAgreement`/`GetAgreementPdf` return `Ok(...)` directly, but the service's `LoadForCallerAsync` throws `NotFoundException` when the tenant-filtered repo returns null (`BookingAgreementService.cs:33-35`), so `Ok()` never receives null — a non-party (or anonymous) caller gets a clean 404, never a 200-with-empty-body and never a probe-able 403. Comment's claim holds.

**Auth / anonymous access — considered, cleared:** the two new endpoints carry no `[HasPermission]`, and B2B has **no** authorization fallback policy (`AddAuthorization()` with no options) and no global authorize filter (`AddInternalControllers` adds none) — so they are anonymous at the pipeline level. This is **not novel to this diff**: the pre-existing `GetById` on the same controller ships the identical no-attribute pattern, and the comment explicitly mirrors it. Authorization is delegated to the two-party tenant filter (`ApplyVenueArtist<BookingAgreementEntity>`), which fails closed for a non-party/no-tenant caller (→ null → `NotFoundException` → 404). No doc rule requires `[HasPermission]` on every endpoint, so not raised as a finding — noted for the record.

**Microservice isolation / module boundaries:** clean — controller depends only on same-module `IBookingAgreementService` (Concert Application) and `Tenant.Contracts` (permissions), no cross-data-service refs. **DTOs vs Responses:** `GetAgreement` returns `BookingAgreementDto` verbatim — the documented default (`api/CLAUDE.md`, "Controllers return either the Dto verbatim … or a Response"). **C# conventions:** explicit ctor + `private readonly` fields (no `_` prefix), validators use ctors; the 2-line comment at the agreement endpoints is a legitimate site-specific WHY (security-model footgun), within the CLAUDE.md comment budget. **Seeding:** n/a.

No issues found in this area (correctness, microservice isolation, module boundaries, seeding, C# conventions all clean).

## Frontend (app/) — reviewed 2026-07-08

Reviewed the `applicationApi` changes (click-wrap flag on apply/accept, new `getAgreementPdf`), the `ApplicationActions.agreement` HATEOAS type add, the new `AgreeToTermsCheckbox` + `useDownloadAgreement` in `b2b/shared`, their re-exports, and the four consuming surfaces (`ApplyAction`, `ArtistApplyCheckoutPage`, `AcceptApplicationPage`, `VenueAcceptCheckoutPage`, `MyApplicationsPage`).

**Web-tier boundaries (the FE analogue of microservice isolation — `app/web/b2b/shared/CLAUDE.md`):** clean.
- `AgreeToTermsCheckbox` and `useDownloadAgreement` live in `app/web/b2b/shared` (both-managers + no-customer). Both are consumed by **two** apps — the checkbox by artist (`ApplyAction`, `ArtistApplyCheckoutPage`) and venue (`AcceptApplicationPage`, `VenueAcceptCheckoutPage`); the download hook by artist (`MyApplicationsPage`) and venue (`AcceptApplicationPage`). Two-consumer bar met, customer cannot resolve `@b2b/*` — correct tier.
- `useDownloadAgreement` carries a legit 2-line WHY (DOM object-URL is web-only, can't live in the cross-platform `@concertable/shared` core) — accurate and within budget.
- `getAgreementPdf` was added to the pre-existing `app/shared/.../concerts/api/applicationApi.ts` (universal tier). The whole `applicationApi`/`concerts` API surface already lived there before this diff, so this follows existing structure rather than introducing a new customer-visible B2B leak — not raised (pre-existing placement).

**Correctness — click-wrap gate is complete and sound ✓.** The API layer posts `agreedToTerms: true` **unconditionally**, so the real consent gate is the disabled submit button. All four surfaces gate correctly:
- `ApplyAction` — checkbox shown only when `!requiresCheckout && canApply`; button `disabled={… || (!requiresCheckout && !agreed)}`; the checkout branch navigates to `ArtistApplyCheckoutPage` (which re-gates).
- `ArtistApplyCheckoutPage` / `VenueAcceptCheckoutPage` — `StripePaymentForm disabled={… || !agreed}`.
- `AcceptApplicationPage` — checkbox shown when `!requiresCheckout` (defined `AcceptApplicationPage.tsx:33`); confirm button `disabled={… || (!requiresCheckout && !agreed)}`.
- **No ungated caller** — grep confirms the only invokers of `applyToOpportunity*`/`acceptApplication`/`useApply`/`useAcceptApplicationMutation` are these four surfaces; nothing can silently assert consent the user never gave (matching the API-layer footgun comment).

**Correctness — misc ✓.** `getAgreementPdf` does the correct axios `responseType: "arraybuffer"` → `new Blob([data], { type: "application/pdf" })`; `useDownloadAgreement` uses the standard object-URL + anchor + `revokeObjectURL` download and gates its buttons on `actions.agreement` + `isPending`. `ApplicationActions.agreement?: ActionLink | null` mirrors the backend HATEOAS `Agreement` link (verified against the API-stage `ApplicationResponseMapper`).

No issues found in this area (correctness, web-tier boundaries, conventions all clean).

## Tests — Concert integration/unit + B2B E2E — reviewed 2026-07-08

Reviewed the new `BookingAgreementApiTests` (16 integration tests: per-contract snapshot immutability, consent capture at Apply, click-wrap 400s, fingerprint-changed 400, pre-consent seeded-app path, PDF download by both parties / 404 for non-party / blob-name-at-Accept, agreement metadata read + HATEOAS link), the new `AgreementTermsRendererTests` unit tests (4 per-contract terms strings), the `agreedToTerms = true` body updates across every existing apply/accept integration + API-E2E test, and the UI E2E page-object/step changes (`AgreeToTermsCheckbox` gating via the new idempotent `LocatorExtensions.EnsureCheckedAsync`).

**Cross-area note (signature change) — resolved.** Verified every `apply`/`accept` POST in the entire B2B test tree now sends `{ agreedToTerms = true }`; the only bodyless POSTs remaining hit `cancel`/`withdraw`/`reject`/`checkout` (no required body). No unconverted call site would silently 400.

**Correctness:** strong. The snapshot-immutability tests genuinely prove the freeze — they mutate the live contract via `IContractModule.UpdateAsync` *after* Accept and re-assert the agreement is unchanged, with a `Assert.Equal(desired, updated)` sanity check that the live row really changed. `EnsureCheckedAsync` is correctly idempotent (checks `aria-checked` before clicking), so the Given-step tick + When-step submit-tick double-call on the same checkbox is a safe no-op, not a flake — matching its own doc comment. **Microservice isolation:** clean — all test code is same-service; `UpdateContractAsync`'s use of `IContractModule` via a DI scope is a same-service module facade, legitimate in a test helper. **Seeding:** clean — no seeder changes; `Accept_ShouldSucceedWithNullArtistConsent_ForPreConsentApplication` correctly documents that seeded apps predate click-wrap (null `ArtistConsent`/`TermsFingerprint`) rather than back-filling consent into the seed.

- [ ] **TEST1 — LOW — correctness (weak assertion)** — `api/Concertable.B2B/src/Modules/Concert/Tests/Concertable.B2B.Concert.IntegrationTests/Application/BookingAgreementApiTests.cs:301`
  `Agreement_Metadata_IsReadableByParty_And404ForStranger` asserts `Assert.Contains("2026-07", body)` with the comment `// platform terms version`. But `BookingAgreementDto` also serializes `CreatedAtUtc` (a `DateTime`; on the test run date `2026-07-08` it renders `"2026-07-08T…"`), so the substring `"2026-07"` is present regardless of whether `PlatformTermsVersion` serializes correctly — the assertion does not actually isolate the version it claims to check. It would still pass if the version field regressed, as long as `CreatedAtUtc` fell in the same month. The exact-value check *does* exist structurally in `AssertCommonSnapshot` (`Assert.Equal("2026-07", agreement.PlatformTermsVersion)`), so the entity is covered — this is purely the metadata **endpoint/DTO-serialization** assertion that's ambiguous. Fix: deserialize to `BookingAgreementDto` and assert `PlatformTermsVersion` directly (as the sibling `AcceptedApplication_ExposesAgreementHateoasLink` test deserializes `ApplicationResponse`), rather than substring-matching the raw body. Judgement call — the coverage exists elsewhere; this is a latent test blind spot, not a live bug.

- [ ] **CV5 — LOW — C# conventions (doc comments)** — `api/Concertable.B2B/tests/E2ETests/Concertable.B2B.E2ETests.Ui/Support/LocatorExtensions.cs:5-6`
  `EnsureCheckedAsync` is documented with a bare `///` doc comment (`/// Ticks a Radix checkbox …`) with no `<summary>` element. `CODE_CONVENTIONS.md` ("Doc comments — XML `<summary>`, not `//`", line 136) says a documented member is written as `/// <summary>…</summary>`. Same category as CV3 (the content itself — the idempotency/overlapping-entry-point rationale — is legitimate WHY worth keeping; just wrap it in `<summary>`). Trivial and in a test-support file; flagged only for consistency with CV3 and the committed convention.

No other issues found in this area (correctness, microservice isolation, module boundaries, seeding all clean).

## Migrations, packages & infra plumbing — reviewed 2026-07-08

Final stage. Reviewed the substantive files (the auto-generated migration renames — pure re-scaffold with new timestamp IDs — were skipped as move-only): the Concert `InitialCreate` migration + `ConcertDbContextModelSnapshot` (BookingAgreement schema, Application consent columns), `appsettings.json` `Legal` section, all six `Directory.Packages.props`, the `Concertable.Messaging.Infrastructure.csproj` EF-Design add, `initial-migrations.ps1`, and the `TECH_DEBT.md` entry.

**Cross-area note (config key) — resolved.** `appsettings.json` defines `"Legal": { "PlatformTermsVersion": "2026-07" }` — the immutable-legal-record field is fed a real value, not `null`, and it matches the value the integration tests assert.

**Correctness:** clean.
- **Migration matches the reviewed model ✓** — the `BookingAgreements` table (all four money columns nullable, `VenueConsent_UserId/AtUtc` NOT NULL + `_Ip/_UserAgent` nullable, `ArtistConsent_*` all nullable, `TermsText`/`PlatformTermsVersion` NOT NULL, `PdfBlobName` nullable) mirrors the `BookingAgreementEntity`/`Consent` config reviewed in the foundation stage exactly. The `Applications` table gained the same `ArtistConsent_*` + `TermsFingerprint` nullable columns (consent-at-Apply). Migration + snapshot are scaffolded together, so consistent by construction.
- **FK / index safe ✓** — `FK_BookingAgreements_Bookings_BookingId` is created with no `onDelete` (default NoAction/Restrict), correct for a permanent legal record (never cascade-delete an agreement); `IX_BookingAgreements_BookingId` is **unique** → one agreement per booking. It's a fresh `InitialCreate` (CreateTable only) — no data-loss/destructive operations.
- **Scaffold plumbing is coherent ✓** — `initial-migrations.ps1` repoints the Messaging Outbox/Inbox contexts' `--startup-project` to the Messaging infrastructure project itself (with a 2-line footgun WHY: a service host as startup would load the *packaged* Messaging assembly, which already contains InitialCreate). This requires design-time scaffolding to run standalone, which is exactly why the same diff adds `Microsoft.EntityFrameworkCore.Design` to `Concertable.Messaging.Infrastructure.csproj` (+ the version pin in Messaging's `Directory.Packages.props`, `PrivateAssets=all`). Self-consistent.

**Package adds:** `QuestPDF 2025.1.3`, `Concertable.Shared.Blob.Application`, `Concertable.Shared.Pdf.Application` in B2B — all agnostic shared libs matching the feature (consistent with the foundation-stage `.csproj` review). Platform version bump `0.1.0-alpha.0.552 → .558` across all six services is a uniform platform-sync, not feature-specific. **Microservice isolation:** clean — no cross-data-service package refs introduced. **TECH_DEBT.md:** the new LOW entry (agreement PDFs share the `images` container; immutability is app-level write-once because a dedicated-container + no-overwrite upload would cross the published `Concertable.Shared.Blob` package boundary the feature was scoped to avoid) is an honest, well-scoped debt log with a clear "resolves when" — correctly placed in the nearest area `TECH_DEBT.md`, not the root.

No issues found in this area (correctness, microservice isolation, module boundaries, seeding, C# conventions all clean).

# Big review (Wave 2) — Feature/BookingAgreement

**Plan anchored to commit:** `769dfcf01654eddb91cb5c5d7c021ab20c514e12`  _(2026-07-12)_
<!-- Continuation of the completed BIG-Feature-BookingAgreement-Review.md (watermark 3cc9f874,
     2026-07-08). This wave stages the 19 commits / 145 files added AFTER that watermark —
     the Consent→ESignature rework, keyed fingerprint (replacing the CV1/CV2 switch),
     dropped write-only agreement columns, owner-vs-public concert read refactor, PDF renames. -->
**Reviewed up to commit:** `769dfcf01654eddb91cb5c5d7c021ab20c514e12`  _(2026-07-12)_
Net diff reviewed: `3cc9f874..769dfcf0` (145 files, +1881/-564). Move-only files skipped
(the `20260622…InitialCreate` migration renames across every service are pure re-scaffolds
from `f7305e83`; only Concert's `20260711120222_InitialCreate` + snapshot changed substantively).
Status legend: `[ ]` not yet reviewed · `[x]` reviewed (date) · `[~]` in progress (incomplete — re-review).

## Coverage
- [x] Concert Domain — ESignature model rework (2026-07-12) — foundation — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain/Entities/ApplicationEntity.cs` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain/Entities/BookingAgreementEntity.cs` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain/Entities/Consent.cs` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Domain/Entities/ESignature.cs`
- [x] Concert Application — interfaces/DTOs/fingerprint components/requests (2026-07-12) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Application`
- [x] Concert Infrastructure — services/PDF/repos/EF configs/workflow (2026-07-12) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Services` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Pdf` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Repositories` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Data/Configurations` `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Extensions`
- [x] Concert API — controllers/mappers/requests/responses/validators + owner-vs-public read (2026-07-12) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Api`
- [x] Frontend (app/) — ESignature panel, concert-page agreement download, ConfigBar/Hero (2026-07-12) — `app/shared/src/features/concerts` `app/web/b2b` `app/web/shared`
- [x] Tests — Concert integration + B2B E2E + shared testing — reviewed 2026-07-12 — `api/Concertable.B2B/src/Modules/Concert/Tests` `api/Concertable.B2B/tests` `api/Concertable.Shared/tests/Concertable.Testing` `api/Concertable.Payment/tests`
- [x] Migrations, packages, docs & plumbing (2026-07-12) — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Data/Migrations` `**/Directory.Packages.props` `**/TECH_DEBT.md` `api/docs/CODE_CONVENTIONS.md` `app/web/CODE_CONVENTIONS.md` `CLAUDE.md` `plans`

## Cross-area notes
<!-- one-liners added during a stage for things a LATER stage must verify -->
- ~~**[for: Concert Infrastructure stage]** `BookingAgreementEntity.VenueESignature` is **required** (non-null; `= null!`). Verify the Accept/build path always supplies a non-null venue `ESignature` (the Wave-1 `VenueConsent` invariant, now re-expressed for signatures).~~ **RESOLVED (2026-07-12)** — `BookingAgreementBuilder.BuildAsync` always constructs `new ESignature(currentUser.Id ?? throw ForbiddenException, timeProvider…, clientContext…, venueESignature.SignatoryName, venueESignature.DrawnSignatureImage)` for the venue side (never null); a null would require `currentUser.Id` null, which throws first. Exactly the Wave-1 pattern, re-expressed for signatures.
- ~~**[for: Concert Infrastructure stage — EF config]** `ESignature.DrawnSignatureImage` is `string?` and will hold a (potentially large) base64/data-URL image. Verify `BookingAgreementEntityConfiguration`/`OpportunityEntityConfiguration` map it as `nvarchar(max)` (unbounded) rather than a default-length column that would truncate/throw on a real drawn signature.~~ **RESOLVED (2026-07-12)** — both configs map the owned signatures with a bare `builder.OwnsOne(a => a.ArtistESignature/VenueESignature)` (no nested `HasMaxLength`), and there is no `ConfigureConventions` string-length cap in B2B. EF's SQL Server default for an unconfigured `string?` is `nvarchar(max)`, and the scaffolded migration confirms it: `ArtistESignature_DrawnSignatureImage` / `VenueESignature_DrawnSignatureImage` are `nvarchar(max), nullable: true`. No truncation.
- ~~**[for: Concert Application + API stages]** `ESignature.SignatoryName` is **required** (non-nullable). Verify the `ESignatureRequest` + its validator require a non-empty name, and the builder passes it through (a blank name would produce a legally-weak signature record).~~ **RESOLVED (2026-07-12, API stage)** — `ESignatureRequestValidator.RuleFor(x => x.SignatoryName).NotEmpty()` is applied to the nested request via `SetValidator` on both `ApplyRequestValidator`/`AcceptRequestValidator`; auto-validation is globally on (Wave-1 confirmed `AddFluentValidationAutoValidation()` + `AddValidatorsFromAssemblyContaining<ApplyRequestValidator>(includeInternalTypes:true)`), so a blank `signatoryName` → 400 "You must sign by entering your full name". The builder-maps-name half was already confirmed in the Infrastructure stage (`BookingAgreementBuilder`/`ApplyExecutor` construct `new ESignature(…, request.SignatoryName, …)`).
- ~~**[for: Concert API stage]** The `ApplyRequest`/`AcceptRequest` validators wire the nested signature check as `.When(x => x.ESignature is not null)` — the `.When(is not null)` guard **skips all signature validation when `ESignature` itself is null**.~~ **RESOLVED (2026-07-12)** — the null case is **explicitly** caught by a separate first rule `RuleFor(x => x.ESignature).NotNull().WithMessage("You must sign the booking agreement")`, which fires before/independently of the `.When`-guarded nested validator. A body omitting `eSignature` → `NotNull()` fails → 400; the `.When(is not null)` on the second rule only prevents an NRE from running the child validator on a null (it is **not** the sole gate). The design is stronger than the note feared — it does not rely on MVC implicit-required (that is only a secondary backstop). No null `ESignature` can reach the executor.
- ~~**[for: Concert Infra PDF + API/DTO + Migrations stages]** The four money columns (`Fee`/`HireFee`/`Guarantee`/`ArtistDoorPercent`) were **removed** from `BookingAgreementEntity`. Verify nothing still reads them.~~ **PDF half RESOLVED (2026-07-12, Infra):** `BookingAgreementDocument` derives money only from `agreement.TermsText`. **API/DTO half RESOLVED (2026-07-12, API):** a `.Fee\b|HireFee|Guarantee|ArtistDoorPercent` grep across the whole `Concert.Api` source tree returns **zero source hits** (only compiled `bin/` DLLs) — no response, mapper, or DTO in the API layer reads a removed money field. **Migrations half remains open** for that stage.
- ~~**[for: Frontend (app/) stage]** The public read `GET /api/Concert/{id}` (`ToDetailsResponse`) now returns **`Actions = null`** (it previously emitted `ConcertActions(Cancel: …)`); owner action links — Cancel **and the new Agreement PDF link** (`/api/Concert/{id}/agreement/pdf`) — are emitted **only** by the tenant-scoped `GET /api/Concert/user/{id}` and `GET /api/Concert/application/{applicationId}` (`ToCurrentUserDetailsResponse`). Wire shape changed too: `ConcertActions` is now `(ActionLink? Cancel, ActionLink? Agreement)` and `ConcertDetailsResponse.Actions` is **nullable**. Verify the FE (a) reads Cancel/Agreement from the owner endpoint, not the public read (else the venue cancel button + agreement download silently disappear), and (b) its `ConcertActions`/`ConcertDetailsResponse` TS types tolerate a null `actions` and carry the new `agreement` link.~~ **RESOLVED (2026-07-12, Frontend stage)** — both halves hold. **(a)** `MyConcertPage` reads its concert from the owner endpoint: `useMyConcert` → `useMyConcertQuery` → `myConcertApi.getMyConcert` → `GET /concert/user/{id}` (moved out of the cross-platform core into `app/web/b2b/shared` — see the boundary note in Findings), and gates the download button on `concert.actions?.agreement`. The public `useConcertQuery` (`["concert", id]`) is never used for owner links; the owner read has its own distinct `["concert","mine",id]` key. **(b)** `Concert.actions` is `actions?: ConcertActions` (optional) and `ConcertActions` now carries `cancel?: ActionLink | null` + `agreement?: ActionLink | null`; every read site uses `?.` so a null `actions` renders no owner buttons rather than throwing.
- ~~**[for: Migrations, packages, docs & plumbing stage]** `BookingAgreementPdfService` now serializes every QuestPDF render behind a process-wide `static SemaphoreSlim renderLock` (a 2-line footgun-comment workaround for QuestPDF's non-thread-safe `GeneratePdf`). Per root `CLAUDE.md` ("a workaround for a library limitation you can't route around … log a line in the nearest `TECH_DEBT.md` naming the root cause"), verify the B2B `TECH_DEBT.md` records this QuestPDF thread-safety workaround (root cause + the process-wide-serialization cost). Not a defect in this stage — the lock is a correct, minimal serialization; only the debt-logging obligation is out-of-stage.~~ **RESOLVED (2026-07-12)** — `api/Concertable.B2B/TECH_DEBT.md` carries a full entry ("PDF render thread-safety guarded in B2B, not in the shared PDF service") that names the root cause (QuestPDF `GeneratePdf` not thread-safe; SkiaSharp font-subset race → unextractable/searchable text), the deterministic repro (clean single-threaded, ~5–10% corrupt under 16-way concurrency), the process-wide-serialization cost, the fact that the shared `Concertable.Shared.Pdf.PdfService` is the real chokepoint (Customer's ticket-receipt PDF still unguarded because the shared lib is a published package this change couldn't republish), and a concrete "Resolves when" exit. Obligation fully met — exceeds the note's ask.

## Findings
<!-- appended per area; finding IDs continue the Wave-1 scheme: MS#, MB#, BUG#, SEED#, CV#, TEST# -->

## Concert Domain — ESignature model rework — reviewed 2026-07-12

Foundation stage of Wave 2. Reviewed the `Consent`→`ESignature` rename (record deleted + added), the `ApplicationEntity.ArtistConsent`→`ArtistESignature` field + `RecordArtistConsent`→`RecordArtistESignature` method rename, and the `BookingAgreementEntity` rework (money columns dropped, `Create` factory de-branched, consent fields → signature fields).

**Headline — Wave-1 `CV1` resolved by this refactor ✓.** `BookingAgreementEntity.Create` no longer casts/`switch`es on contract subtypes to flatten money (`(contract as FlatFeeContract)?.Fee` etc. and the `ArtistDoorPercent` switch are gone, along with the four `decimal?` columns). The factory now stores the typed contract agnostically — exactly the direction Wave-1 flagged. The domain factory is contract-agnostic again; no keyed-strategy inconsistency remains here.

**C# conventions:** the new `ESignature` record carries a proper `/// <summary>` doc comment (contrast the Wave-1 `CV3`/`CV5` `/* */`-and-bare-`///` nits) — form-correct. The retained `/* Null = … */` one-liners on `ArtistESignature` are short inline WHY notes (not member docs), unchanged in style from Wave-1 and within the comment budget — not re-flagged.

**Microservice isolation / module boundaries:** clean — Domain references only its own types + `B2B.Contract.Contracts` (`IContract`/`FlatFeeContract`/etc.), no cross-data-service refs. **Seeding:** n/a (write-model entity, created in the Accept transaction).

No issues found in this area. The four cross-area notes above are verification hooks for later stages, not defects here.

## Concert Application — interfaces/DTOs/fingerprint components/requests — reviewed 2026-07-12

Reviewed the `ESignatureRequest` addition, the `ESignatureRequest` threading through every Apply/Accept interface (`IApplicationService`, `IApplyDispatcher`, `IAcceptanceDispatcher`, `IApplyExecutor`, `IAcceptExecutor`, `IBookingAgreementBuilder`), the `ConsentDto`→`ESignatureDto` rename (+ `SignatoryName`) and the `ArtistConsent`/`VenueConsent`→`ArtistESignature`/`VenueESignature` DTO field renames, the new keyed `IContractFingerprintComponent` + four concrete components + `TermsFingerprintCalculator` refactor, and the `GetByConcertIdAsync`/`GetDetailsByIdAsync`/`GetDetailsForCurrentUserAsync` finder/service additions (owner-vs-public concert read).

**Headline — Wave-1 `CV2` resolved by this refactor ✓.** `TermsFingerprintCalculator.Calculate` no longer `switch`es on concrete contract subtypes to extract the deal numbers. It is now a textbook keyed-strategy resolver (`CODE_PATTERNS.md` → "Keyed strategy resolver"): concrete strategies injected as ctor params (`FlatFeeFingerprintComponent flatFee, …`), a `FrozenDictionary<ContractType, IContractFingerprintComponent>` mapping all four keys, and `components[contract.ContractType].Compose(contract)` delegating with a loud `KeyNotFoundException` on an unmapped key. It mirrors the sibling `AgreementTermsRenderer`/`ContractMapper` shape exactly. All four components are DI-registered (`ServiceCollectionExtensions.cs:89-92`), so the calculator resolves. The per-strategy hard casts (`((FlatFeeContract)contract).Fee`) are correct for the pattern — routing is by `ContractType` key, so the concrete type is guaranteed. The calculator legitimately implements a *different* interface from its components (`ITermsFingerprintCalculator` vs `IContractFingerprintComponent`) because it adds real logic on top (payload composition + SHA-256), not a pure pass-through facade — a sound variation, not a deviation.

**DTOs / Requests:** `ESignatureRequest` is correctly placed in `Application/Requests/`, `internal sealed record` with `{ get; init; }` and a `required` name (`api/CLAUDE.md` "Service write inputs are `Request` types … Request records use `{ get; init; }`"). `ESignatureDto`/`BookingAgreementDto` stay `internal`. Renames are mechanical and consistent. **Microservice isolation / module boundaries:** clean — Application references only its own Domain/Requests + `B2B.Contract.Contracts` (`IContract`/`ContractType`/concrete contracts); no cross-data-service refs. **Seeding:** n/a.

- [ ] **CV6 — LOW — C# conventions (doc comments)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Application/Interfaces/IBookingAgreementRepository.cs:13-14`
  The newly-added `GetByConcertIdAsync` finder is documented with a `/* … */` block comment rather than `/// <summary>`. This is the **same category as Wave-1 `CV3`** (which flagged the two existing `/* */` finder comments in this same interface) — new code in this wave continues the file's local `/* */` style instead of the `CODE_CONVENTIONS.md` "Doc comments — XML `<summary>`" convention. The content (the tenant-filter/why-null rationale) is legitimate WHY worth keeping; just convert to `<summary>`, ideally alongside the CV3 conversions so the whole interface is consistent. Trivial; flagged only for completeness since it's a Wave-2 addition.

No other issues found in this area (correctness, microservice isolation, module boundaries, seeding all clean). The two cross-area notes for the API stage (SignatoryName non-emptiness confirmation + the `.When(ESignature is not null)` null-skip guard) are verification hooks for that stage, not defects here.

## Concert Infrastructure — services/PDF/repos/EF configs/workflow — reviewed 2026-07-12

Reviewed the `Consent`→`ESignature` propagation through the whole Accept/Apply write path (`ApplicationService` public signatures, `ApplyDispatcher`/`AcceptanceDispatcher`, `ApplyExecutor`/`AcceptExecutor`, `BookingAgreementBuilder`), the two EF owned-type config renames (`BookingAgreementEntityConfiguration`, `OpportunityEntityConfiguration`), the four fingerprint-component DI registrations, the `BookingAgreementDocument` PDF rework (Consent→Signatures section + drawn-image decode), the `renderLock` serialization in `BookingAgreementPdfService`, the new `GetByConcertIdAsync` / `GetPdfByConcertIdAsync` / owner-read `ConcertRepository.GetDetailsByIdAsync` + `ConcertService.GetDetailsForCurrentUserAsync`, and the `ESignatureDto` service mapping.

**All three inbound cross-area notes resolved** (see Cross-area section): venue e-signature is always non-null at build; `DrawnSignatureImage` maps `nvarchar(max)` (config default + migration confirmed); the PDF derives money only from `TermsText` (no removed money columns).

**Correctness — the two new reads fail closed ✓.**
- `BookingAgreementRepository.GetByConcertIdAsync` queries the **tenant-filtered** `context.BookingAgreements` (base is `VenueArtistTenantScopedRepository`), correlating to the concert's booking via `context.Concerts.Any(…)`. A non-party's tenant filter yields no agreement → null → `NotFoundException` in `GetPdfByConcertIdAsync` → 404. No `IgnoreQueryFilters`.
- `ConcertRepository.GetDetailsByIdAsync` reads the **public/unfiltered** `Concerts` but scopes visibility by requiring a **tenant-visible `Booking`** (`context.Bookings.Any(b => b.Id == e.BookingId)`; `Bookings` is tenant-filtered) — composing visibility rather than subtracting it (`CODE_PATTERNS.md` "Tenancy is composed, never subtracted"). Non-party sees no booking → null → 404. Fail-closed, consistent with the sibling agreement read.

**Correctness — e-signature capture ✓.** Artist signature captured at Apply (`ApplyExecutor.RecordArtistESignature`, ambient server attribution + request `SignatoryName`/`DrawnSignatureImage`), venue signature captured at Accept (`BookingAgreementBuilder`, same shape). Server always owns `UserId`/`AtUtc`/`Ip`/`UserAgent` from the request-scoped `IClientContext`/`ICurrentUser` — never the client — exactly as the `ESignature` doc-comment mandates. Atomicity unchanged from Wave-1 (build only tracks; the transitioner's single `SaveChangesAsync` commits transition + agreement + signatures together).

**renderLock ✓.** `RenderUploadAsync` wraps `pdfService.Render(...)` in `await renderLock.WaitAsync()` / `try … finally { renderLock.Release() }` — a correct, minimal process-wide serialization for QuestPDF's non-thread-safe render. Over-serialization (safe), not under. TECH_DEBT-logging obligation forwarded to the docs stage (cross-area note).

**Microservice isolation / module boundaries:** clean — all deps are same-service (Concert Application/Domain/Data + `Shared.Blob`/`Shared.Pdf`/`Kernel` agnostic adapters); no cross-data-service refs. **Keyed strategy:** the four `*FingerprintComponent` singletons are the concrete strategies for the Wave-2 `TermsFingerprintCalculator` resolver (reviewed in the Application stage) — registered as concrete types, facade as `ITermsFingerprintCalculator`; matches `CODE_PATTERNS.md`. **Seeding:** n/a.

- [ ] **BUG1 — LOW — correctness (swallowed failure signal)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Infrastructure/Pdf/BookingAgreementDocument.cs:134-135`
  `DecodeDrawnSignature` swallows a `FormatException` and returns `null` (`try { return Convert.FromBase64String(payload); } catch (FormatException) { return null; }`), so a **non-null but undecodable** stored drawn signature is silently rendered as "no drawn image" on a *legal* agreement PDF. Root `CLAUDE.md` ("Don't default away a failure … an empty `catch` … that turns an invalid value into a benign one hides the case that should surface") — freshly re-emphasised in HEAD commit `a004a39c` — targets exactly this shape. _Counter-argument (why LOW, a judgement call):_ letting the exception propagate would hard-fail the whole agreement download for **both** parties over a display-only glyph, while the legally-material data (`SignatoryName`/`AtUtc`/`UserId`/`Ip`) still renders, and `DrawnSignatureImage` is explicitly optional ("only adds perceived weight" per the `ESignature` doc). The fix that satisfies both: **log a warning** (a `[LoggerMessage]` in the project `Log.cs`, per `CODE_CONVENTIONS.md`) on the decode failure so a corrupt stored signature surfaces, then degrade — rather than a fully silent swallow. Author's call.

No other issues found in this area. **Not flagged (considered):** the two `/* … */` block comments added here (`DecodeDrawnSignature`, `ConcertRepository.GetDetailsByIdAsync`) are implementation-level WHY notes on a private helper / repo impl — root `CLAUDE.md` permits short inline WHY notes there; they are **not** interface member-docs, so they are a different category from the Wave-1 `CV3`/`CV5` and Wave-2 `CV6` findings (which flag `/* */` on **interface** contract surfaces where `/// <summary>` is the convention). The `GetDetailsByIdAsync` note runs to 3 lines — marginally over the ≤2-line budget — but carries a genuine fail-closed-security WHY worth keeping; within tolerance, not raised.

## Concert API — controllers/mappers/requests/responses/validators + owner-vs-public read — reviewed 2026-07-12

Reviewed the `ApplyRequest`/`AcceptRequest` reshape (`AgreedToTerms bool` → nested `ESignatureRequest`), the new `ESignatureRequestValidator` + the two request validators' rewire, the `ApplicationController` Apply/Accept plumbing of `request.ESignature`, the two new `ConcertController` endpoints (`GET user/{id}` owner read, `GET {id}/agreement/pdf`), the `GetDetailsByApplicationId` switch to the current-user response, the `ToCurrentUserDetailsResponse` mapper + `ConcertActions`/`ConcertDetailsResponse` shape change, the `DevController` accept-signature stub, and the `"POST"`/`"GET"` → `HttpMethods.Post`/`.Get` cleanup across all four response mappers.

**All three inbound cross-area notes resolved** (see Cross-area section): the `SignatoryName` `NotEmpty()` validator fires under global auto-validation; the null-`ESignature` case is caught explicitly by `NotNull()` (the `.When(is not null)` guard is only NRE-avoidance, not the gate); no API-layer source reads a removed money column. One new note added forward for the Frontend stage (public read now returns `Actions = null`).

**Correctness — the click-wrap → e-signature validator is sound ✓.** Both `ApplyRequestValidator`/`AcceptRequestValidator` now run two rules on `x.ESignature`: `NotNull()` (missing/`null` signature → 400 "You must sign the booking agreement") and a `.When(x => x.ESignature is not null)`-guarded `SetValidator(new ESignatureRequestValidator())` (present-but-blank name → 400 "You must sign by entering your full name"). The `NotNull()` rule is the real null gate, so the `.When` guard skipping the child on null is correct (avoids an NRE) rather than a hole. `ESignatureRequestValidator` runs as a manually-instantiated child validator, so it needs no DI registration; the two parent validators are picked up by the Wave-1-confirmed `AddValidatorsFromAssemblyContaining<ApplyRequestValidator>(includeInternalTypes:true)` under global `AddFluentValidationAutoValidation()`.

**Correctness — owner-vs-public read split fails closed ✓.**
- Both current-user reads resolve through the **tenant-scoped** `IConcertRepository` (`ConcertService.GetDetailsForCurrentUserAsync` and the pre-existing `GetDetailsByApplicationIdAsync` both use `repository`, not `publicRepository`), so a non-party gets `null` → `NotFoundException` → 404 **before** any owner action link is emitted. Only `GetDetailsByIdAsync` (`GET {id}`) uses `publicRepository`, and it now maps via `ToDetailsResponse` → `Actions = null`. Switching `GetDetailsByApplicationId` to `ToCurrentUserDetailsResponse` is therefore safe — that endpoint was already party-scoped.
- **Unconditional Agreement PDF link is safe ✓.** `ToCurrentUserDetailsResponse` emits `Agreement` non-null for every concert. Verified a `ConcertEntity` can never be read before its `BookingAgreement` exists: the only concert-creation path is `ConcertEntity.CreateDraft` in `ConcertDraftService.CreateAsync`, driven by `CreateConcertDraftStep` **from a Booking** — and the Booking + agreement are produced together at Accept (Wave-1: agreement built atomically with the Accept transition), one step before the concert draft. So the link never dangles to a 404 on a real concert.
- **New endpoints anonymous-but-fail-closed ✓.** `GET user/{id}`, `GET {id}/agreement/pdf` carry no `[HasPermission]` — consistent with the documented B2B pattern (no auth fallback policy; visibility delegated to the tenant filter, which 404s non-parties). `GetAgreementPdf` returns `File(pdf.Content, "application/pdf", pdf.FileName)`; `GetPdfByConcertIdAsync` throws `NotFoundException` on a tenant-miss (reviewed in the Infra stage), so `File(...)` never receives null.

**Microservice isolation / module boundaries:** clean — `ConcertController` gains only same-module deps (`IBookingAgreementService`, `IConcertService`, `IConcertWorkflowModule` — all Concert Application / workflow-module facade); the Api-layer `ApplyRequest`/`AcceptRequest` compose the Application-layer `ESignatureRequest` (an Api→Application cross-layer reference within the *same* service, which is correct — `ESignatureRequest` is the service write input, correctly homed in `Application/Requests/` per `api/CLAUDE.md`). No cross-data-service refs. **DTOs vs Responses:** `ConcertDetailsResponse` is the bounded-exception public details Response (`api/CLAUDE.md`) — the `Actions?`-nullable + `Agreement`-link change is an intended wire-shape evolution of that frozen contract, and `GetAgreement`/`GetAgreementPdf` return the Dto/file verbatim. **Seeding:** n/a.

- [ ] **CV7 — LOW — C# conventions (comment budget / duplicated rationale)** — `api/Concertable.B2B/src/Modules/Concert/Concertable.B2B.Concert.Api/Mappers/ConcertResponseMappers.cs:71-74`
  The `ToCurrentUserDetailsResponse` comment runs to **4 lines** (root `CLAUDE.md`: code comments are "≤2 lines … Anything longer belongs in the commit message"), and it **restates the same security-model rationale** — "tenant-scoped endpoint ⇒ reaching it proves you're a party ⇒ emit party-only links; the permission on POST cancel is the role gate, not this" — already written at the `GetDetailsForCurrentUser` endpoint in `ConcertController.cs:34-36`. Root `CLAUDE.md` flags exactly this ("two copies drift the day one changes"). _Counter-argument (why LOW, a judgement call):_ the reasoning is a genuine fail-closed-security WHY worth keeping *somewhere*, and the Infra stage already accepted a 3-line security WHY within tolerance — so this is consistency housekeeping, not a defect. Fix: keep one authoritative note (the controller endpoint is the better site — it's where the "no `[HasPermission]`" decision lives) and trim the mapper to a ≤2-line pointer, or drop it. The 3-line `GetDetailsForCurrentUser` controller comment on its own is within the same tolerance the Infra stage applied — not separately raised.

No other issues found in this area (correctness, microservice isolation, module boundaries, seeding all clean). **Not flagged (considered):** the 2-line `/* … */` note on `ApplyRequest`/`AcceptRequest` (`ApplicationRequests.cs`) is a short design-rationale WHY (why `agreedToTerms` is gone — the signature's presence *is* the consent), on a record type rather than an interface member-doc surface, and within the 2-line budget — a different category from the `/// <summary>` CV findings; not raised. The `HttpMethods.Post`/`.Get` swap for string literals is a pure, correct convention improvement (typed constants over magic strings) with no behaviour change.

## Frontend (app/) — ESignature panel, concert-page agreement download, ConfigBar/Hero — reviewed 2026-07-12

Reviewed the click-wrap → e-signature UI rework across all three web tiers: the shared `applicationApi` reshape (`agreedToTerms: true` → nested `eSignature: ESignatureRequest` on apply / apply-with-payment / accept) + new `concertApi.getAgreementPdf`, the `ConcertActions.agreement` type addition, the deleted `AgreeToTermsCheckbox` and new `ESignaturePanel` + `SignatureCanvas` (b2b/shared), the `useDownloadAgreement` switch from application-id to concert-id, the `useMyConcert`/`useMyConcertQuery`/`myConcertApi` move into b2b/shared, the artist `ApplyAction` sign-&-apply dialog + `useApply`, the two checkout pages and `AcceptApplicationPage`, and the universal `ConfigBar` action slot + `Hero`/`useBannerTextColor` change. Rules applied: `app/web/CODE_CONVENTIONS.md`, `app/web/shared/CLAUDE.md`, `app/web/b2b/shared/CLAUDE.md`.

**Headline — the owner-vs-public boundary is now correct ✓ (the high-value FE check).** The owner concert read moved **out of** the cross-platform `@concertable/shared` core (`app/shared/.../hooks/useMyConcert.ts` deleted, its `index.ts` export removed) **into** `app/web/b2b/shared` (`useMyConcert` reimplemented + new `useMyConcertQuery` + `myConcertApi`). This is exactly right per `app/web/shared/CLAUDE.md`: `GET /concert/user/{id}` is a **tenant-scoped B2B** call that 404s non-parties and carries party-only action links — the customer core must not compile a hook that makes it. `myConcertApi` even documents the rationale. The download button lives in b2b (`MyConcertPage`), gated on `concert.actions?.agreement`, reading the owner endpoint (never the public `["concert", id]` read) — resolving the inbound cross-area note (a)+(b).

**Correctness — signature capture + validation parity ✓.** Every write path threads the client's `ESignatureRequest`; the server stamps user/time/IP (confirmed backend-side in the API/Infra stages). FE submit gating matches the backend `SignatoryName` `NotEmpty()`: `ApplyAction`, both checkout pages, and the non-checkout `AcceptApplicationPage` all disable submit on `eSignature.signatoryName.trim() === ""`. The checkout accept path (`AcceptApplicationPage.handleConfirm`) navigates to the checkout page and returns **before** any `mutate` — it never fires an accept with an empty signature; the signature is captured on `VenueAcceptCheckoutPage`. The drawn image is optional on both sides.

**Microservice isolation / module boundaries ✓.** No `role`/`isVenueManager` branching added to shared; the universal `ConfigBar` takes variation through a generic `actions?: ReactNode` slot (managers inject the download+cancel buttons) — the documented slot pattern, not a leak. `ESignaturePanel`/`SignatureCanvas` correctly sit in b2b/shared (both manager apps, customer can't resolve; the panel imports `@b2b/features/contracts`). No customer-only or app-only literal landed in a shared tier. **Seeding:** n/a (frontend).

**Hero / `useBannerTextColor` deletion — an improvement, not a regression ✓.** Replacing the runtime luminance sampler with a fixed gradient overlay + `text-white`/`text-shadow` also removes a real Tailwind footgun: the old code built class names dynamically (`text-${textColor}`), which the JIT compiler doesn't emit unless safelisted. Static classes are strictly safer. Legibility is now guaranteed by the gradient rather than sampled per-image.

- [ ] **CV8 — LOW — dead code (leftover after the refactor)** — `app/shared/src/features/concerts/api/applicationApi.ts:66-72`
  `applicationApi.getAgreementPdf` (application-scoped, `GET /application/{id}/agreement/pdf`) is now **unused** — a repo-wide grep finds the only agreement-download caller (`useDownloadAgreement`) switched to `concertApi.getAgreementPdf` (concert-scoped), and the application-scoped download buttons were removed from `MyApplicationsPage` and `AcceptApplicationPage`. The method is not *broken* (the backend `ApplicationController.GetAgreementPdf` route still exists), just orphaned FE code. Remove it. _Cross-area observation (backend stages already `[x]`, so recorded here not as a note):_ the backend counterparts are now also FE-orphaned — `ApplicationController` still exposes `GET {id}/agreement/pdf` and `ApplicationActions` still emits `Agreement` (`ApplicationResponses.cs:16`), which no FE surface reads anymore. Confirm whether the application-scoped route + `ApplicationActions.Agreement` are intentionally retained (e.g. for an API consumer) or should be dropped alongside the FE method.

- [ ] **CV9 — LOW — frontend conventions (write-input type in `api/` not `types.ts`)** — `app/shared/src/features/concerts/api/applicationApi.ts:7-12`
  The new `ESignatureRequest` interface is declared **inline in the `api/` file** rather than the feature's `types.ts`. `app/web/CODE_CONVENTIONS.md` states "Contract types live in the feature's `types.ts` — reads and `XRequest`s alike; `api/xApi.ts` **imports** them, it never declares its own." This is *already listed* in that doc's "Violations at a glance" table (#3, naming `ESignatureRequest (applicationApi.ts:7)`), so it is tracked/acknowledged debt rather than a new discovery — flagged only for completeness as a Wave-2 addition. Fix: move `ESignatureRequest` to `app/shared/src/features/concerts/types.ts` and re-export from there (the b2b `index.ts` re-export and all `type ESignatureRequest` imports then just repoint).

No other issues found in this area. **Not flagged (considered):** the `!` non-null assertions in `useMyConcert` (`draft!.name`, `toggleEdit(concert!)`) are a straight move of the pre-existing shared implementation, guarded by `MyConcertPage`'s `if (!concert) return skeleton` and the `isDirty` gate on save — not a new null-defaulting footgun. `concertApi.getAgreementPdf` living in the all-sites `app/shared` core matches its sibling manager-only methods there (`cancelConcert`, `updateConcert`) and is never *invoked* from the customer app (only defined), so it does not trip the `app/web/shared` "runs every call it makes" test — pre-existing placement shape, not raised. The 1-2 line `/* … */` notes added to `myConcertApi`/`useMyConcertQuery`/`ESignaturePanel`/`SignatureCanvas`/`ConfigBar` are short WHY notes on implementation surfaces (not interface member-docs) within the comment budget — a different category from the `/// <summary>` CV findings.

## Tests — Concert integration + B2B E2E + shared testing — reviewed 2026-07-12

Reviewed the click-wrap → e-signature migration across the whole test suite (the mechanical `agreedToTerms = true` → `eSignature = new { signatoryName = "Test Signatory" }` swap in every Apply/Accept POST, both integration and E2E), the `BookingAgreementApiTests` rework (dropped money-column assertions + renamed `Apply_ShouldRecordArtistESignatureAndFingerprint` + two new `Agreement_Pdf_*` PDF-text tests + `ExtractPdfText` helper + the `PdfPig` package ref), the new `TenantScopingTests.CurrentUserConcertRead_*` owner-vs-public test, the `Actions.Cancel` → `Actions!.Cancel` null-forgiving updates (integration + Payment E2E), the UI E2E page-object rework (`agree-to-terms` checkbox → `e-sign` fill; `VenueDetailsPage` two-step apply-then-sign dialog; `MyConcertPage.DownloadAgreementAsync`; `FlatFeeWorkflow.feature` download step), and the two E2E fixture changes (`AppFixture.WaitForSpasServingAsync` + `StripeCardEntry` scroll/click reorder). Rules applied: `code-review` Lens A (test correctness) + the E2E-readiness guidance in root `CLAUDE.md`.

**Headline — the new tests are strong, real-behaviour assertions ✓.**
- `Agreement_Pdf_RendersBothPartyESignatures` / `Agreement_Pdf_RendersPlaceholder_ForPreESignArtistSignature` fetch the real PDF over HTTP and extract text with PdfPig, asserting the party signature lines *and* the "No recorded signature (predates e-sign)" placeholder — genuine content verification, not a status-code smoke test. Distinct signatory names ("Zola Banks"/"Marco Vento") tie each extracted line to its party. `ExtractPdfText` even asserts the `%PDF` magic bytes first. These directly retire the Wave-1 `TEST1` weak-assertion concern (the racy `PdfBlobName` assert is deliberately dropped — see the retained comment at `BookingAgreementApiTests.cs` — with the PDF lifecycle now covered by these dedicated tests).
- `TenantScopingTests.CurrentUserConcertRead_ScopesActionsToPartiesAndKeepsPublicReadActionFree` verifies all four visibility quadrants: both parties get the owner action links (incl. the exact `agreement/pdf` href), a stranger tenant gets **404 not 403** (deal existence hidden), and the public read returns `Actions = null`. Exactly the owner-vs-public split the API/Frontend stages introduced — well covered.

**Migration correctness ✓.** The dropped money-column asserts (`Assert.Equal(500m, agreement.Fee)` etc.) are correctly removed — those columns no longer exist (Domain stage, Wave-1 `CV1` resolution) — and the frozen-snapshot proof now rides `TermsText` (which still contains the agreed `£500.00`/`70%`/etc.), so the "edit-after-accept doesn't mutate the frozen agreement" tests still assert the amount, via text. The two "must sign" tests correctly moved from `agreedToTerms = false` to `signatoryName = ""` (blank name → `NotEmpty()` → 400), preserving intent against the new validator.

**E2E fixture changes are proper fixes, not workarounds ✓.** `AppFixture.WaitForSpasServingAsync` polls each SPA until it serves 200 and **throws on timeout** before the browser drives it — the correct answer to Aspire's `AddNpmApp` having no HTTP readiness probe (an upstream limitation), not a `sleep`, and it surfaces failure rather than defaulting it away. This is the pattern root `CLAUDE.md` endorses over the warm-up-hack shape it warns against.

**Microservice isolation / module boundaries:** clean — all test refs are same-service (`B2B.Concert.Domain`, `B2B.Contract.Contracts`, `B2B.IntegrationTests.Fixtures`); `PdfPig` is a test-only reader, already pinned in `api/Concertable.B2B/Directory.Packages.props` (v0.1.15), so the bare `<PackageReference Include="PdfPig" />` resolves under CPM. **Seeding:** n/a — tests drive the real Apply/Accept HTTP flow; no direct `context.X.AddRange`.

No issues found in this area (no ≥80-confidence defects). **Not flagged (considered):** the `Actions!.Cancel` null-forgiving operators are fail-loud in test code (an unexpected null `Actions` throws NRE → red test), the correct reaction to the now-nullable `ConcertActions`. `MyConcertPage.DownloadAgreementAsync`'s `?? ""` on the content-type header flows straight into the `!Contains("application/pdf")` throw — it surfaces a missing/wrong content-type, not a default-away. The `StripeCardEntry` scroll-before-click reorder is a standard Playwright interaction-stability tweak on a test helper (no behaviour under test changes); a 1-line magic-ordering change without comment, but below the flag threshold. The 3-line `WaitForSpasServingAsync` comment marginally exceeds the ≤2-line budget but carries a genuine upstream-limitation WHY at the exact site — within the tolerance prior Wave-2 stages applied.

**Anchor-drift note (out of this stage's range):** this review covered `3cc9f874..769dfcf0` per the plan anchor. HEAD (`a004a39c`) is 2 commits past it — including `219b1981 test(b2b): assert e-signatures in the agreement PDF via the UI E2E`, a **further test change** this stage did **not** cover. It (and the docs commit `a004a39c`) should be picked up by an `incremental-review` after this wave completes — do not assume the Tests area is covered to HEAD.

## Migrations, packages, docs & plumbing — reviewed 2026-07-12

Final stage of Wave 2. Reviewed the Concert `20260711120222_InitialCreate` migration + snapshot (Consent→ESignature, money-column drop), the `PdfPig` package add, the B2B + root `TECH_DEBT.md` entries, the `CODE_CONVENTIONS.md` / `CLAUDE.md` doc additions, and the four `plans/` changes. Rules applied: `code-review` migration-correctness lens + the plans/TECH_DEBT hygiene rules in root + `plans/CLAUDE.md`.

**Both inbound cross-area obligations resolved** (see Cross-area section):
- **renderLock TECH_DEBT ✓** — `api/Concertable.B2B/TECH_DEBT.md` records the QuestPDF non-thread-safe `GeneratePdf` workaround in full (root cause = SkiaSharp font-subset race producing unextractable text, deterministic repro, the shared-lib chokepoint with Customer's ticket PDF still unguarded, and a concrete "Resolves when"). Exceeds the note's ask.
- **Money-columns migration half ✓** — the migration + snapshot drop `Fee`/`HireFee`/`Guarantee`/`ArtistDoorPercent` cleanly; a grep across the migration finds zero references to them.

**Migration correctness ✓.** It's a nuke-and-rescaffold `InitialCreate` (dev/E2E only, per `api/CLAUDE.md`), and the create-table is internally consistent with the model snapshot: no money columns; `ESignature` owned columns present; `VenueESignature_{UserId,AtUtc,SignatoryName}` non-null (the required venue-signature invariant confirmed in the Domain/Infra stages), `ArtistESignature_*` nullable (the optional artist signature — the "predates e-sign" placeholder the Tests stage covered). The owned-type nullability asymmetry is standard EF (an optional owned reference forces its columns nullable even where the property is `IsRequired`; the required venue reference stays non-null) — correct, not a defect.

**Packages ✓.** `PdfPig 0.1.15` added to `api/Concertable.B2B/Directory.Packages.props` (alphabetical placement); consumed test-only under CPM as a bare `<PackageReference>` (confirmed in the Tests stage). **Microservice isolation / module boundaries / seeding:** n/a — migration/packages/docs only, no cross-service or write-model code.

No issues found in this area (no ≥80-confidence defects). **Not flagged (considered):**
- **`plans/PDF_RENDERER_RENAME.md` and `plans/b2b/DEAL_RENAME.md`** are *future*-work plans (both explicitly scheduled for `Refactor/*` branches off master **after** `Feature/BookingAgreement` merges) — outstanding work, not completed-and-orphaned. Correctly committed on this branch (docs are branch-hygiene exempt). Not rot.
- **`plans/b2b/CONCERT_OWNER_READ_REFACTOR.md`** describes work *implemented in this branch* — a candidate for `git rm` under the "delete the plan in the commit that completes it" rule. But its STATUS honestly records one **genuinely-outstanding** gate (the UI E2E regress, env-blocked locally by the `initial-migrations.ps1` "already an object named 'Outbox'" issue now logged in `api/TECH_DEBT.md`, deferred to the merge-queue E2E gate). Per the plans rule a *partially-done* plan legitimately stays until its verification lands, and its status is accurate rather than misleading — so retention is defensible. Worth deleting once the merge-queue E2E goes green; not a defect.
- **`api/TECH_DEBT.md` `initial-migrations.ps1` entry** correctly captures the re-stamp/desync root cause behind the E2E-startup failure — the "environment problem" the repo's E2E rule anticipates, properly logged rather than papered over.
- Doc prose (`CLAUDE.md` workaround guidance, `api/docs/CODE_CONVENTIONS.md` optional-parameter rule, the new 294-line `app/web/CODE_CONVENTIONS.md`) is content, not code — outside the correctness/boundary lenses; the `LAUNCH_PLAN.md` line edit accurately restates the shipped advanced-tier e-signature scope.

---

## Summary — Wave 2 complete (all 7 areas reviewed 2026-07-12)

Net diff `3cc9f874..769dfcf0` (145 files) reviewed across 7 area-stages. The two Wave-1 headline concerns are **resolved by this wave's refactor**: `CV1` (money-flattening `switch` in `BookingAgreementEntity.Create`) is gone with the money columns; `CV2` (contract-subtype `switch` in `TermsFingerprintCalculator`) is now a textbook keyed-strategy resolver. The owner-vs-public concert read split and its FE boundary move (`useMyConcert` out of the customer core into `app/web/b2b/shared`) are correct and fail-closed; e-signature capture is server-attributed and validated on both tiers; the new PDF tests assert real extracted content.

**Findings: 5, all LOW, no must-fix.**
- **BUG1** (correctness) — `BookingAgreementDocument.DecodeDrawnSignature` silently swallows a `FormatException` on an undecodable stored drawn signature (renders as "no image" on a legal PDF). Fix = log a warning then degrade, rather than a fully silent swallow. Author's call (display-only glyph; legally-material fields still render).
- **CV6 / CV7 / CV9** (C# + FE conventions) — `/* */` block comment where `/// <summary>` is the convention (`IBookingAgreementRepository`); a 4-line duplicated-rationale comment (`ConcertResponseMappers`); a write-input type declared in `api/` instead of `types.ts` (`ESignatureRequest`, already tracked in the FE conventions doc).
- **CV8** (dead code) — orphaned `applicationApi.getAgreementPdf` after the switch to concert-scoped download; plus an observation that the backend application-scoped `GET {id}/agreement/pdf` route + `ApplicationActions.Agreement` are now FE-orphaned — confirm intentional or drop.

**Cross-area notes:** all resolved; none left open.

**Anchor drift (handoff):** this wave covered up to the plan anchor `769dfcf0`. HEAD is `a004a39c`, **2 commits past** it (`219b1981` — a further B2B UI-E2E e-signature assertion; `a004a39c` — docs). Continue past the watermark below with `incremental-review`.

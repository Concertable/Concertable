---
name: comments
description: The C# mechanics of comments and XML documentation — `//` for a one-line why and a single `/* */` block for the rare multi-line one (never stacked `//` lines), comment placement, when a `<summary>` earns its place at all, documenting the interface and never its implementation, `<see cref>`/`<c>` cross-references, and how to open a summary so it says what the thing is. Use when adding or reviewing a comment or XML doc on a C# type or member, or when deciding whether a member needs documenting.
---

# comments

The standard is `../../standards/dotnet/COMMENTS.md`, shipped in this plugin. Read it and follow it; this skill only routes to it.
